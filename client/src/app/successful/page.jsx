import { Button } from "@/components/ui/button";

export default function Successful() {
  return (
    <div className="flex items-center justify-center min-h-screen">
      <Button variant="secondary">Successful ✅</Button>
    </div>
  );
}